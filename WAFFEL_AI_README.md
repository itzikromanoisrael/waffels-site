# WAFFEL AI

עותק עבודה חדש ונפרד של אתר Waffel’s.

מטרה:

- לנסות שינויים חדשים באתר בלי לגעת ישירות בתיקייה הפעילה `WAFFELS_PUBLISH_MOBILE`.
- כל שינוי חדש שנבצע מעכשיו עבור רעיון “WAFFEL AI” יתבצע כאן.

מקור העותק:

- `..\WAFFELS_PUBLISH_MOBILE`

מה לא הועתק:

- `.git` המקורי, כי האתר המקורי הוא Git worktree.
- `node_modules`
- `.env`
- קבצי `.log`

הרצה מקומית:

```powershell
$env:PORT='3001'
$env:HOST='127.0.0.1'
node backend/server.js
```

כתובת מומלצת לעבודה על העותק:

`http://127.0.0.1:3001/`

